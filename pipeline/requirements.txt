# PC pipeline deps (one-shot run_pc.py + real-time run_realtime.py).
# STT/translate model deps (torch, transformers, ...) come from
# stt/requirements.txt and translate/requirements.txt.
soundfile>=0.12.0
librosa>=0.10.0
piper-tts>=1.2.0
onnxruntime>=1.17.0
python-dotenv>=1.0.0
PyYAML>=6.0
numpy>=1.24.0

# Real-time only (docs/05-vad-realtime-integration.md)
sounddevice>=0.4.6
webrtcvad>=2.0.10

# --mode reason needs reason/requirements.txt too (bitsandbytes):
#   pip install -r reason/requirements.txt
# Not included via -r here on purpose — bitsandbytes needs a CUDA-matched
# build and shouldn't be a hard dependency for translate-only setups.
# See docs/reasoningModel/01-gemma-reasoning-mode.md §16.
