# PC pipeline deps (one-shot run_pc.py + real-time run_realtime.py).
# STT/translate model deps (torch, transformers, ...) come from
# stt/requirements.txt and translate/requirements.txt.
soundfile>=0.12.0
librosa>=0.10.0
piper-tts>=1.2.0
onnxruntime>=1.17.0
python-dotenv>=1.0.0
PyYAML>=6.0
numpy>=1.24.0

# Real-time only (docs/05-vad-realtime-integration.md)
sounddevice>=0.4.6
webrtcvad>=2.0.10
